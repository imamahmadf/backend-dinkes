const express = require("express");
const { publikControllers } = require("../controllers");
const router = express.Router();

// router.get("/get", publikControllers.getPublik);

module.exports = router;
